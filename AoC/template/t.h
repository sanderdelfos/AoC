#pragma once
#include "Input.h"

class  : public Puzzle
{
public:
	explicit ()
		: Puzzle()
	{
	}

	int partOne(const Input& input) override
	{
		if (input.lines.empty())
			return -1;


		return -1;
	}

	int partTwo(const Input& input) override
	{
		if (input.lines.empty())
			return -1;


		return -1;
	}
};
